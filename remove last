Node* removeLastNode(Node* head)
{
    if (head == NULL)
        return NULL;
 
    if (head->next == NULL) {
        delete head;
        return NULL;
    
    
node* temp=head; 
while(temp->next !=null)
{
temp=temp->next;
}
delete(temp->next);
temp->next=NULL;
return head;
}
